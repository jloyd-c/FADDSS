from django.apps import AppConfig

class ResidentPortalConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.resident_portal'
    verbose_name = 'Resident Portal'